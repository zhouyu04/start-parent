<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzyy.start.mapper.AopLogMapper">


    <insert id="saveSysLog" parameterType="com.zzyy.start.entity.SysLog">
        INSERT INTO sys_log(username,operation,`time`,method,params,ip)VALUES (
            #{username},#{operation},#{time},#{method},#{params},#{ip}
        )
    </insert>

</mapper>